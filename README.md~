# BDD-package


# Usage
To run the package, execute ./bdd-package.jar without any parameters.
On windows, you may need to use the command prompt, and the command "java -jar bdd-package.jar" to execute the file. When giving parameters, you might need to enclose your parameter in quotes.

Usage will be explained in the console.
Source code can be found in /src.

#Summary
A simple implementation of a package for handling BDDs.
This uses graphstream (http://graphstream-project.org/) for displaying the BDDs, jbool_expressions (https://github.com/bpodgursky/jbool_expressions) to parse strings to expressions.
